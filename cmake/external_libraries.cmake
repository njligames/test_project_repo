if(EMSCRIPTEN)
  message(STATUS "Platform is emscripten")
  add_subdirectory(emscripten)
elseif(WINDOWS)
  if(ARCH_64)
    message(STATUS "Platform is Windows 64 bit")
    add_subdirectory(windows64)
  else()
    message(STATUS "Platform is Windows 32 bit")
    add_subdirectory(windows32)
  endif()
elseif(APPLE)
  if(IOS)
    message(STATUS "Platform is iOS")
    add_subdirectory(ios)
  elseif(TVOS)
    message(STATUS "Platform is appletv")
    add_subdirectory(appletv)
  else()
    message(STATUS "Platform is MacOS")
    add_subdirectory(macos)
  endif()
elseif(UNIX AND NOT APPLE AND NOT ANDROID)
  if(LINUX)
    message(STATUS "Platform is Linux")
    add_subdirectory(linux)
  elseif(UNIX)
    message(STATUS "Platform is Unix")
    add_subdirectory(unix)
  endif()
elseif(ANDROID)
  message(STATUS "Platform is Android")
  add_subdirectory(android)
endif()


